#include <iostream>
#include <vector>
#include <fstream>
#include <string>

using namespace std;

// Funcao principal
int main()
{
    carregarTarefa();
    int alternativa;
    do
    {
        /* code */
    } while (alternativa != 5);
    return 0;
}